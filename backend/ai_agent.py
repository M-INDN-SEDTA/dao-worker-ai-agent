def summarize_task(text: str) -> str:
    """
    Simulated AI summary logic.
    Replace with actual AI API call (OpenAI/AI21/etc) if available.
    """
    # Simple placeholder summary: first 100 characters
    summary = text[:100] + ("..." if len(text) > 100 else "")
    return summary
